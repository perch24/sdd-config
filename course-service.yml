server:
  port: 8102

spring:
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            mvc:
              method: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"

