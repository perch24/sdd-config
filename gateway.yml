server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lowerCaseServiceId: true

gateway:
  swagger:
    refreshRate: 5000
    ignoredRoutes:
      - configserver

